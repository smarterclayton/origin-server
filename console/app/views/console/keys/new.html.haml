- content_for :title, "Add a public SSH key"
- breadcrumb_for_settings 'Add a public SSH key'
%h1 Add a public key
=flashes

= render :partial => 'console/keys/form', :locals => {:key => @key, :first => @first}
